/** Demo landing hero — dark theme (`public/landing-page/`). */
export const LANDING_DEMO_HERO_IMAGE_DARK = "/landing-page/globe.png" as const;

/** Demo landing hero — light theme (`public/landing-page/`). */
export const LANDING_DEMO_HERO_IMAGE_LIGHT = "/landing-page/globe-light.png" as const;
